{
  "name": "neec-docker",
  "version": "1.0.0",
  "description": "docker sample",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "M.Katsube",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2"
  }
}
